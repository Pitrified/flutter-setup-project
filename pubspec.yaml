name: fala
description: Conversational language tutor with structured corrections
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_gemma:
  flutter_riverpod:
  flutter_secure_storage:
  freezed_annotation:
  go_router:
  hive:
  hive_flutter:
  json_annotation:
  openai_dart:
  path_provider:

dev_dependencies:
  build_runner:
  custom_lint:
  flutter_lints:
  flutter_test:
    sdk: flutter
  freezed:
  http:
  integration_test:
    sdk: flutter
  json_serializable:
  riverpod_lint:

flutter:
  uses-material-design: true
  assets:
    - assets/prompts/
    - assets/prompts/tutor_response/
    - assets/fixtures/
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
